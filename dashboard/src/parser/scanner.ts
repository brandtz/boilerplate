/**
 * Layer 1: File Scanner
 *
 * Walks directory tree for known paths, filters for .md and .json files,
 * returns file path + raw content pairs.
 *
 * Implementation deferred to prompt 12.0.1.
 */
