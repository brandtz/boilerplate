/**
 * Next-Prompt Selection Algorithm
 *
 * Filters eligible prompts (ready + prerequisites met + not blocked),
 * sorts by sequence, selects the next prompt, and generates rationale.
 *
 * Implementation deferred to prompt 13.0.1.
 */
