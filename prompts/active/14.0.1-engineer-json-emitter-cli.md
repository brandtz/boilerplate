---
prompt_id: "14.0.1"
title: "Engineer — JSON State Emitter and CLI (E1-S6)"
phase: "implementation"
status: "ready"
epic_id: "E1"
story_id: "E1-S6"
task_ids:
  - "E1-S6-T1"
  - "E1-S6-T2"
  - "E1-S6-T3"
  - "E1-S6-T4"
owner_role: "Senior Software Engineer"
prerequisites:
  - "13.0.1"
required_reading:
  - "schemas/dashboard-state.example.json"
  - "docs/project-manager-dashboard-data-contract.md"
  - "agents/decisions/ADR-002-parser-architecture.md"
  - "docs/technical-tasks-e1-parser.md"
  - "agents/standards/prompt-lifecycle-standard.md"
  - "agents/standards/handoff-standard.md"
downstream_prompts:
  - "15.0.1"
inserted_after: null
affects_prompts: []
review_required: []
created_at: "2026-04-03T00:00:00Z"
updated_at: "2026-04-03T00:00:00Z"
session_handoff: ""
supersedes: ""
superseded_by: ""
insert_reason: ""
completed_at: ""
archived_at: ""
---

# Prompt 14.0.1: Engineer — JSON State Emitter and CLI (E1-S6)

## Sequence Position

- Prompt: 14.0.1
- Epic: E1
- Story: E1-S6
- Tasks: E1-S6-T1 through T4
- Phase: Implementation

## Prerequisites

- 13.0.1 complete (Sorting and dependency engine)

## Context for the Agent

You are the Senior Software Engineer. Implement the JSON state emitter that serializes the normalized graph model into the `dashboard-state.example.json` schema. Create a CLI entry point that accepts a repo path and outputs the JSON file.

## Required Reading

```text
schemas/dashboard-state.example.json
docs/project-manager-dashboard-data-contract.md
agents/decisions/ADR-002-parser-architecture.md
docs/technical-tasks-e1-parser.md
```

## Scope

- Implement JSON serialization of DashboardState
- Conform output to the schema defined in dashboard-state.example.json
- Create CLI entry point: `npx dashboard-parse --repo <path> --output <json-path>`
- Write snapshot tests comparing emitter output to expected JSON
- Test with the actual repo as input (self-referential test)

## Constraints

- Output must be deterministic (same repo = same JSON)
- CLI must validate repo path exists and is dashboard-capable before parsing

## Validation Commands

```bash
cd apps/dashboard && npx dashboard-parse --repo ../.. --output /tmp/state.json
cat /tmp/state.json | jq '.summary.epicsTotal'
```

## Handoff Instructions

Create handoff notes. Include: CLI usage, test results, sample output snippet.


## Session Close-Out Checklist

Before committing, the executing agent MUST complete ALL of the following steps.
This checklist is mandatory for both local and cloud agents.

1. **Create session handoff** using `agents/templates/session-handoff-template.md`
   - Save to `agents/handoffs/S-<date>-<seq>-<slug>.md`

2. **Update this prompt's frontmatter:**
   - Set `status: "done"`
   - Set `session_handoff` to the handoff file path created in step 1
   - Set `completed_at` to the current ISO-8601 timestamp
   - Set `updated_at` to the current ISO-8601 timestamp

3. **Update `prompts/index.md`:**
   - Change this prompt's row: status to `done`, add handoff path, set completed date, add summary note
   - Update the summary counts (decrement ready, increment done)

4. **Update `agents/context/status-dashboard.md`:**
   - Add this prompt to Active Work Packets as **DONE** with a brief note
   - Update Metrics counts (Prompts Completed, ready/done tallies)
   - Update Next Suggested Action to the next prompt in sequence
   - Update Awaiting Review if any items were resolved or added

5. **Update `agents/context/decision-log.md`:**
   - Add a row for each decision made during this session

6. **Commit and push:**
   ```bash
   git add -A
   git commit -m "Complete prompt <prompt_id>: <title>"
   git push
   ```
## Downstream Consumers

- 15.0.1 (Parser code review)
- 16.0.1+ (UI consumes the emitted state)

## Stop Conditions

- STOP if the parser cannot read the current repo structure
